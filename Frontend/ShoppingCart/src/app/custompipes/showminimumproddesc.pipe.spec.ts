import { ShowminimumproddescPipe } from './showminimumproddesc.pipe';

describe('ShowminimumproddescPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowminimumproddescPipe();
    expect(pipe).toBeTruthy();
  });
});
